# Optional: USDA FoodData Central API key (free, takes ~1 minute to get)
# Sign up at: https://fdc.nal.usda.gov/api-key-signup.html
# Without a key, food search uses OpenFoodFacts only.
# After adding this file, restart the dev server (`npm run dev`).
FDC_API_KEY=
